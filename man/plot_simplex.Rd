% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_simplex.R
\name{plot_simplex}
\alias{plot_simplex}
\title{Plot a simplex (ternary) diagram}
\usage{
plot_simplex(
  df,
  x_col,
  y_col,
  z_col,
  color_col = NULL,
  size_col = NULL,
  col_palette = NULL,
  xlab = "x",
  ylab = "y",
  zlab = "z",
  title = "title"
)
}
\arguments{
\item{df}{A data frame where each row is a point to plot.}

\item{x_col}{Character name of the column in \code{df} for the first axis (bottom-left corner).}

\item{y_col}{Character name of the column in \code{df} for the second axis (bottom-right corner).}

\item{z_col}{Character name of the column in \code{df} for the third axis (top corner).}

\item{color_col}{Character name of the column in \code{df} for point colors. Default \code{NULL}.}

\item{size_col}{Character name of the column in \code{df} for point sizes. Default \code{NULL}.}

\item{col_palette}{Named vector of colors passed to \code{scale_color_manual}. Default \code{NULL}.}

\item{xlab}{Label for the first axis (bottom-left corner). Default \code{"x"}.}

\item{ylab}{Label for the second axis (bottom-right corner). Default \code{"y"}.}

\item{zlab}{Label for the third axis (top corner). Default \code{"z"}.}

\item{title}{Plot title. Default \code{"title"}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a ternary plot for three-way compositional data using ggplot2,
without requiring the ggtern package.
}
\details{
Rows whose three values sum to zero have no composition to plot and are
dropped, with a warning naming how many went.
}
\examples{
df <- data.frame(a = c(1, 0, 0, 1, 2),
                 b = c(0, 1, 0, 1, 1),
                 c = c(0, 0, 1, 1, 1),
                 group = c("x", "y", "z", "mixed", "mixed"))
plot_simplex(df = df, x_col = "a", y_col = "b", z_col = "c",
             color_col = "group", title = "")
}
