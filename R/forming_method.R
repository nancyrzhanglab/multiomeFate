.form_estimation_matrices <- function(mat_x, mat_y, ht_neighbor, 
                                      form_options){
  stopifnot(forming_options[["method"]] == "literal")
  
  # initialize
  
  # enumerate all the neighbors, and add to the matrices row-by-row
  # augment matrices if needed
}